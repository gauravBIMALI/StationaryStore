@* @model ClzProject.ViewModels.AdminUserViewModel
@{
    ViewData["Title"] = "User Details";
    // Layout = "~/Views/Shared/_Layout.cshtml"; Adjust path as needed
}

<h2 class="mt-3">User Details</h2>
<hr />

<div class="card shadow p-4">
    <div class="row mb-3">
        <div class="col-md-6">
            <strong>Full Name:</strong> @Model.FullName
        </div>
        <div class="col-md-6">
            <strong>Username:</strong> @Model.UserName
        </div>
    </div>

    <div class="row mb-3">
        <div class="col-md-6">
            <strong>Email:</strong> @Model.Email
        </div>
        <div class="col-md-6">
            <strong>Email Confirmed:</strong> @(Model.EmailConfirmed ? "Yes" : "No")
        </div>
    </div>

    <div class="row mb-3">
        <div class="col-md-6">
            <strong>Phone Number:</strong> @Model.PhoneNumber
        </div>
        <div class="col-md-6">
            <strong>Role:</strong> @Model.Role
        </div>
    </div>

    @if (Model.Role == "Seller" && !string.IsNullOrEmpty(Model.BusinessName))
    {
        <div class="row mb-3">
            <div class="col-md-6">
                <strong>Business Name:</strong> @Model.BusinessName
            </div>
        </div>
    }

    <div class="text-end mt-4">
        <a asp-controller="Admins" asp-action="Index" class="btn btn-secondary">Back to List</a>
    </div>
</div> *@
